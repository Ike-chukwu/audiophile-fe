import React from "react";
import './Spinner.css'

const Spinner = () => {
  return (
    <div className="loader">
      <div className="loader-inner"></div>
    </div>
  );
};

export default Spinner;
